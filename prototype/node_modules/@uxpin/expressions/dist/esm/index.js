import moment from 'moment';
import capitalize from 'lodash/capitalize';
import inRange from 'lodash/inRange';
import forEach from 'lodash/forEach';
import random from 'lodash/random';
import round from 'lodash/round';
import sortBy from 'lodash/sortBy';
import reduce from 'lodash/reduce';
import trim from 'lodash/trim';
import _max from 'lodash/max';
import _min from 'lodash/min';
import { getOS } from './helpers/getOS';
export { capitalize, trim, random, round };
export function replace(source, from, to) {
    if (source === void 0) { source = ''; }
    if (from === void 0) { from = ''; }
    if (to === void 0) { to = ''; }
    return String(source).replace(new RegExp(String(from), 'g'), to);
}
export function indexOf(source, searchValue) {
    if (source === void 0) { source = ''; }
    if (searchValue === void 0) { searchValue = ''; }
    return String(source).indexOf(searchValue);
}
export function length(value) {
    if (value === void 0) { value = ''; }
    return String(value).length;
}
export function left(source, value) {
    if (source === void 0) { source = ''; }
    if (value === void 0) { value = 0; }
    return source.substr(0, value);
}
export function right(source, value) {
    if (source === void 0) { source = ''; }
    if (value === void 0) { value = 0; }
    return source.substr(-value);
}
export function substring(source, start, _length) {
    if (source === void 0) { source = ''; }
    if (start === void 0) { start = 0; }
    if (_length === void 0) { _length = 0; }
    return source.substr(start, _length);
}
export function lowerCase(value) {
    return ("" + (value || '')).toLowerCase();
}
export function upperCase(value) {
    return ("" + (value || '')).toUpperCase();
}
export function concat() {
    var values = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        values[_i] = arguments[_i];
    }
    return values.join('');
}
export function min() {
    var values = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        values[_i] = arguments[_i];
    }
    return _min(values) || 0;
}
export function max() {
    var values = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        values[_i] = arguments[_i];
    }
    return _max(values) || 0;
}
export function abs(value) {
    return Math.abs(value);
}
export function floor(value) {
    return Math.floor(value);
}
export function ceil(value) {
    return Math.ceil(value);
}
export function sqrt(value) {
    return Math.sqrt(value);
}
export function pow(base, exponent) {
    return Math.pow(base, exponent);
}
export function sin(value) {
    return Math.sin(value);
}
export function cos(value) {
    return Math.cos(value);
}
export function tan(value) {
    return Math.tan(value);
}
export function asin(value) {
    return Math.asin(value);
}
export function acos(value) {
    return Math.acos(value);
}
export function atan(value) {
    return Math.atan(value);
}
export function regex(value, regexValue) {
    return new RegExp(regexValue).test("" + value);
}
export function notMatchRegex(value, regexValue) {
    return !regex(value, regexValue);
}
export function isEmpty(value) {
    return "" + value === '';
}
export function isNotEmpty(value) {
    return !isEmpty(value);
}
export function isBetween(value, start, end) {
    return inRange(value, start, end);
}
export function isNotBetween(value, start, end) {
    return !isBetween(value, start, end);
}
export function contains(value, searchString) {
    return ("" + value).indexOf("" + searchString) !== -1;
}
export function notContain(value, searchString) {
    return !contains(value, searchString);
}
export function avg() {
    var values = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        values[_i] = arguments[_i];
    }
    return reduce(values, function (sum, num) { return sum + num; }, 0) / values.length;
}
export function windowWidth() {
    return window.innerWidth;
}
export function windowHeight() {
    return window.outerHeight;
}
export function windowScrollX() {
    console.warn('Works only in UXPin');
    return 0;
}
export function windowScrollY() {
    console.warn('Works only in UXPin');
    return 0;
}
export function deviceOS() {
    return getOS();
}
export function canvasWidth() {
    console.warn('Not working outside UXPin context');
    return 0;
}
export function canvasHeight() {
    console.warn('Not working outside UXPin context');
    return 0;
}
export function pageName() {
    console.warn('Not working outside UXPin context');
    return '';
}
export function pageNumber() {
    console.warn('Not working outside UXPin context');
    return '';
}
export function pagePath() {
    console.warn('Not working outside UXPin context');
    return '';
}
export function getActiveState(elementPath) {
    console.warn('Not working outside UXPin context');
    return '';
}
export function prototypeName() {
    console.warn('Not working outside UXPin context');
    return '';
}
export function now(dateFormat) {
    if (dateFormat === void 0) { dateFormat = ''; }
    return moment().format(dateFormat);
}
export function time(dateFormat) {
    if (dateFormat === void 0) { dateFormat = 'HH:mm:ss'; }
    return moment().format(dateFormat);
}
export function date(dateFormat) {
    if (dateFormat === void 0) { dateFormat = 'YYYY-MM-DD'; }
    return moment().format(dateFormat);
}
export function dateAdd(dateVal, amount, unit, dateFormat) {
    if (unit === void 0) { unit = 'd'; }
    if (dateFormat === void 0) { dateFormat = 'YYYY-MM-DD'; }
    return moment(dateVal).add(amount, unit).format(dateFormat);
}
export function dateSubtract(dateVal, amount, unit, dateFormat) {
    if (unit === void 0) { unit = 'd'; }
    if (dateFormat === void 0) { dateFormat = 'YYYY-MM-DD'; }
    return moment(dateVal).subtract(amount, unit).format(dateFormat);
}
export function dateDiff(firstDate, secondDate, unit) {
    if (unit === void 0) { unit = 'd'; }
    var result = moment(firstDate).diff(secondDate, unit);
    if (isNaN(result)) {
        return 'Invalid date';
    }
    return result;
}
export function dateIsBefore(firstDate, secondDate, precision) {
    if (precision === void 0) { precision = null; }
    return moment(firstDate).isBefore(secondDate, precision);
}
export function dateIsAfter(firstDate, secondDate, precision) {
    if (precision === void 0) { precision = null; }
    return moment(firstDate).isAfter(secondDate, precision);
}
export function dateIsBetween(firstDate, secondDate, thirdDate, precision) {
    if (precision === void 0) { precision = null; }
    return moment(firstDate).isBetween(secondDate, thirdDate, precision);
}
export function median() {
    var values = [];
    for (var _i = 0; _i < arguments.length; _i++) {
        values[_i] = arguments[_i];
    }
    var sortedValues = sortBy(values);
    if (sortedValues.length % 2 === 0) {
        return (sortedValues[sortedValues.length / 2] + sortedValues[sortedValues.length / 2 - 1]) / 2;
    }
    return sortedValues[(sortedValues.length - 1) / 2];
}
export function trunc(value) {
    return parseInt("" + value, 10);
}
export function log(value) {
    return Math.log(value);
}
export function isTrue(value) {
    return value === 'true' || value === true;
}
export function isFalse(value) {
    return value === 'false' || value === false;
}
export function toFixed(value, _digits) {
    if (_digits === void 0) { _digits = 0; }
    // digits is number from range [0..20]
    var digits = Math.min(Math.max(0, _digits), 20);
    return Number(value).toFixed(digits);
}
export function toString(value) {
    return String(value);
}
export function toNumber(value) {
    return Number(value);
}
export function equals(first, second) {
    if ((first === true && second === 'true') || (first === false && second === 'false')) {
        return true;
    }
    return first === second;
}
export function notEqual(first, second) {
    return !equals(first, second);
}
export function format(data, stringFormat) {
    if (!stringFormat) {
        return String(data);
    }
    var index = 0;
    var formattedString = '';
    forEach(stringFormat, function (char) {
        if (char === '?') {
            formattedString += String(data)[index] || '';
            index += 1;
        }
        else {
            formattedString += char;
        }
    });
    return formattedString;
}
