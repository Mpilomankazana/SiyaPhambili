export var Systems;
(function (Systems) {
    Systems["ANDROID"] = "android";
    Systems["IOS"] = "ios";
    Systems["LINUX"] = "linux";
    Systems["MACOS"] = "macosx";
    Systems["UNKNOWN"] = "unknown";
    Systems["WINDOWS"] = "windows";
    Systems["WINDOWS_PHONE"] = "windows phone";
})(Systems || (Systems = {}));
var testers = [
    {
        os: Systems.WINDOWS_PHONE,
        test: function (ua) {
            return ua.indexOf('windows phone') !== -1 || ua.indexOf('xblwp') !== -1 || ua.indexOf('zunewp') !== -1;
        },
    },
    {
        os: Systems.WINDOWS,
        test: function (ua) {
            return !!ua.match(/\bwindows nt ([0-9.]+)/);
        },
    },
    {
        os: Systems.MACOS,
        test: function (ua) {
            return !!ua.match(/\bmac os x ([0-9._]+)/);
        },
    },
    {
        os: Systems.IOS,
        test: function (ua) {
            return !!ua.match(/\bcpu(?: iphone)? os/);
        },
    },
    {
        os: Systems.ANDROID,
        test: function (ua) {
            return !!ua.match(/\bandroid[ \/-]?([0-9.x]+)?/);
        },
    },
    {
        os: Systems.LINUX,
        test: function (ua) {
            return ua.indexOf('linux') !== -1;
        },
    },
];
export function getOS() {
    var ua = navigator.userAgent.toLowerCase();
    var length = testers.length;
    for (var i = 0; i < length; i += 1) {
        if (testers[i].test(ua)) {
            return testers[i].os;
        }
    }
    return Systems.UNKNOWN;
}
